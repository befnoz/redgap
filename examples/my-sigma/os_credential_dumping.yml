title: OS Credential Dumping (mimikatz)
id: aaaa1111-0000-4000-8000-000000000002
status: experimental
description: >
    Tagged to OS Credential Dumping (T1003.008) so it LOOKS like coverage — but it only
    matches a Windows tool name, so on real Linux telemetry it never fires. RedGap flags it
    SILENT: false confidence a unit test written against the author's own log cannot catch.
tags:
    - attack.credential-access
    - attack.t1003.008
logsource:
    product: linux
    category: process_creation
detection:
    selection:
        Image|endswith: '/mimikatz'
    condition: selection
falsepositives:
    - Unknown
level: high
